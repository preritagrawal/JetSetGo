import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        borderWidth: 1,
        height: 55,
        borderRadius: 8,
        borderColor: 'grey',
        marginBottom: 10,
      }
  });

  export default styles;
